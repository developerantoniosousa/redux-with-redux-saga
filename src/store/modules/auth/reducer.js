const INITIAL_STATE = {
  info: [],
  isLoading: false,
  hasError: false,
};

export default function authReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
